# chant run list
# on a copy of examples/local-op-quickstart; see test/level0-goldens/harness.ts for what is normalised
exit: 0

--- wrote (empty)

--- stdout
NAME                      PHASES  DEPENDS             OVERVIEW
azure-deploy              4       —                   az group ensure → build → arm dep...
hello                     1       —                   Minimal local Op — one shell step...
local-aws-migrate         4       —                   Deploy locally, then a gated one-...
local-aws                 4       —                   Floci up → build → cfn deploy → F...
local-gcp                 6       —                   floci-gcp up → build bucket → gcp...
local-k8s                 4       —                   k3d up → apply → wait → k3d down:...

--- stderr (empty)
