// Generated by scripts/generate-composite-catalogs.ts (#2662) from this
// lexicon's composite exports. Do not edit by hand: run
// `npm run generate:composite-catalogs`. catalog.test.ts holds it to the exports.
import type { CompositeEntry } from "@intentius/chant/lexicon";

export const compositeCatalog: CompositeEntry[] = [
  {
    "name": "FlyOtelCollector",
    "lexicon": "fly",
    "description": "Create a FlyOtelCollector composite.",
    "bundles": [
      "Machine"
    ],
    "params": [
      {
        "name": "name",
        "type": "string",
        "required": false,
        "description": "Machine name (default: \"otel-collector\")."
      },
      {
        "name": "app",
        "type": "string | Declarable",
        "required": false,
        "description": "The app the machine belongs to, as an app name."
      },
      {
        "name": "region",
        "type": "string | Declarable",
        "required": false,
        "description": "Region (default: `Fly.Region`, which the build resolves from `FLY_REGION`)."
      },
      {
        "name": "image",
        "type": "string",
        "required": false,
        "description": "Collector image (default: the contrib image at the otel lexicon's pinned collector version)."
      },
      {
        "name": "config",
        "type": "Iterable<Declarable>",
        "required": false,
        "description": "The collector config, as otel lexicon entities (components and pipelines)."
      },
      {
        "name": "exporters",
        "type": "OTelComponent<\"exporter\", string, any>[]",
        "required": false,
        "description": "Exporters for the default config (default: one `debug` exporter)."
      },
      {
        "name": "signals",
        "type": "Signal[]",
        "required": false,
        "description": "Signals the default config has pipelines for (default: traces, metrics and logs)."
      },
      {
        "name": "cpuKind",
        "type": "string",
        "required": false,
        "description": "Guest CPU kind (default: \"shared\")."
      },
      {
        "name": "cpus",
        "type": "number",
        "required": false,
        "description": "Guest CPUs (default: 1)."
      },
      {
        "name": "memoryMb",
        "type": "number",
        "required": false,
        "description": "Guest memory in MB (default: 512)."
      },
      {
        "name": "defaults",
        "type": "{ machine?: Partial<Record<string, unknown>>; }",
        "required": false,
        "description": "Per-member defaults for fine-grained overrides."
      }
    ]
  }
];
