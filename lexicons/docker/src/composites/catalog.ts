// Generated by scripts/generate-composite-catalogs.ts (#2662) from this
// lexicon's composite exports. Do not edit by hand: run
// `npm run generate:composite-catalogs`. catalog.test.ts holds it to the exports.
import type { CompositeEntry } from "@intentius/chant/lexicon";

export const compositeCatalog: CompositeEntry[] = [
  {
    "name": "DockerOtelCollector",
    "lexicon": "docker",
    "description": "Create a DockerOtelCollector composite.",
    "bundles": [
      "DockerConfig",
      "Service"
    ],
    "params": [
      {
        "name": "image",
        "type": "string",
        "required": false,
        "description": "Collector image (default: the contrib image at the otel lexicon's pinned collector version)."
      },
      {
        "name": "config",
        "type": "Iterable<Declarable>",
        "required": false,
        "description": "The collector config, as otel lexicon entities (components and pipelines)."
      },
      {
        "name": "exporters",
        "type": "OTelComponent<\"exporter\", string, any>[]",
        "required": false,
        "description": "Exporters for the default config (default: one `debug` exporter)."
      },
      {
        "name": "signals",
        "type": "Signal[]",
        "required": false,
        "description": "Signals the default config has pipelines for (default: traces, metrics and logs)."
      },
      {
        "name": "publishPorts",
        "type": "boolean",
        "required": false,
        "description": "Publish the receiver ports on the host (default: true)."
      },
      {
        "name": "restart",
        "type": "string",
        "required": false,
        "description": "Restart policy (default: \"unless-stopped\")."
      },
      {
        "name": "defaults",
        "type": "{ service?: Partial<Record<string, unknown>>; config?: Partial<Record<string, unknown>>; }",
        "required": false,
        "description": "Per-member defaults for fine-grained overrides."
      }
    ]
  },
  {
    "name": "DockerWebService",
    "lexicon": "docker",
    "description": "One HTTP service as a Compose service, with a published port and an HTTP health check.",
    "bundles": [
      "Service"
    ],
    "params": [
      {
        "name": "build",
        "type": "{ context: string; dockerfile?: string }",
        "required": false,
        "description": "Build the image from a Dockerfile: the context, and the Dockerfile's path in it (default: \"Dockerfile\")."
      },
      {
        "name": "image",
        "type": "string",
        "required": false,
        "description": "The image to run, or the tag to give the built one."
      },
      {
        "name": "port",
        "type": "number",
        "required": true,
        "description": "The port the app listens on in the container."
      },
      {
        "name": "hostPort",
        "type": "number",
        "required": false,
        "description": "The port published on the host (default: `port`)."
      },
      {
        "name": "environment",
        "type": "Record<string, string>",
        "required": false,
        "description": "More environment variables."
      },
      {
        "name": "healthPath",
        "type": "string",
        "required": false,
        "description": "An HTTP path the health check fetches with `wget` inside the container (default: no health check)."
      },
      {
        "name": "restart",
        "type": "string",
        "required": false,
        "description": "Restart policy (default: \"unless-stopped\")."
      },
      {
        "name": "defaults",
        "type": "{ service?: Partial<Record<string, unknown>>; }",
        "required": false,
        "description": "Per-member defaults for fine-grained overrides."
      }
    ]
  }
];
