{
  "name": "docker-basic-app-example",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "chant build src --lexicon docker -o dist/",
    "lint": "chant lint src",
    "dev": "chant build src --lexicon docker --watch -o dist/"
  },
  "dependencies": {
    "@intentius/chant-lexicon-docker": "workspace:*",
    "@intentius/chant": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
