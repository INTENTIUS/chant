# The body of this file is the live block's content, with no wrapper block
# (choudoufu's live/MARKERS.md and site/content/docs/use/setup.md). The sidecar
# form rather than the in-block one, because choudoufu's own migration guide
# calls it the leading form: stock tooling never reads this file, so adopting
# live markers adds one file and changes zero existing lines.
estate = "chant-adopt-fixture"
