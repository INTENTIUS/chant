on: push
jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: node:20-alpine
    services:
      - name: postgres:15
        alias: postgres
        variables:
          POSTGRES_PASSWORD: secret
    steps:
      - run: npm test
