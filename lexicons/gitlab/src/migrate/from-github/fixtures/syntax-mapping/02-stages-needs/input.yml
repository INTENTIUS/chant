on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: make build
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: make test
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: make deploy
