---
title: "Resources"
description: "The six fountain workload kinds the lexicon declares."
diataxis: reference
---

{/* GENERATED-BY-CHANT-DOCS by `npm run docs -w @intentius/chant-lexicon-fountain` — DO NOT EDIT.
    Edit lexicons/fountain/docs/pages/resources.mdx instead; changes here are overwritten. */}


The lexicon types six kinds. Environment, Vault and Agent are what `fountain
apply` reconciles; Teammate, Schedule and Webhook are the team, schedule and
webhook routes. Types are generated from a pinned fountain release's OpenAPI
spec, so they track the real API. Conversations are deliberately **not** a
resource: they are runs with a status lifecycle, modeled as ops.

## Environment

A reusable sandbox baseline: packages, repos, setup script, env vars,
encrypted secrets, and the networking policy. `networking_type` is
`"unrestricted" | "limited"`; under `limited`, egress is restricted to
`networking_config.allowed_hosts`, and with no hosts (or an empty list) the
sandbox denies all egress — a deny-all, not an allow-all. FTN010 requires the
networking intent to be explicit.

`setup_timeout_seconds` bounds how long the setup script may run, from 1 to 900
seconds with a default of 120. FTN024 checks the range at build, since the
generated type is a plain `number`. Changing the value invalidates the
environment's checkpoints.

`secrets` is a list of `{ key, value }` pairs, and Vault takes the same field.
Neither request schema has it; it comes from fountain's manifest format, and
`fountainApply` sends it as the inline `secrets` map. Values are write-only, so
write a reference that resolves at build, never a literal (FTN001).

## Vault

A bag of env-var overrides selected at conversation create. Vault values
win on key collision with the environment — which is why agents can carry
a vault allowlist upstream (fountain#136). `allowed_vault_ids` on an Agent is
three-state: `null` allows any tenant vault, `[]` forbids all, a list is an
allowlist.

## Agent

A named, re-runnable agent configuration: model, runtime, skills (inline
SKILL.md or GitHub-sourced with a `ref` pin), MCP servers, and an optional
`environment` reference — typed, so a dangling reference is a build error,
not a 422 at apply time. `sandbox_mode` picks between a sandbox per
conversation and one persistent computer per agent identity.

### The ACP runtime

`runtime: "acp"` launches `runtime_command` inside the sandbox and speaks the
Agent Client Protocol to it. Both are in fountain's spec from v0.21.0, which
made `model` optional for that runtime alone. FTN023 keeps the two together.
FTN016 rejects a `model` on an acp agent, because the model is whatever the
command on the other end of the protocol chooses, and rejects an agent on any
other runtime that has no `model`, which fountain refuses at apply.

`inference_credential_id` and `allowed_inference_credential_ids` name
inference credential sets by uuid. The lexicon does not model the sets
themselves, so these stay ids rather than typed references.

## Teammate

An Agent seated on the team, with a conversation of its own that becomes that
seat's operational history. `agent` is a typed reference; `environment` and
`vault` override the agent's own for this seat, and must satisfy its
`allowed_environment_ids` and `allowed_vault_ids`. FTN021 rejects a dangling
reference at build rather than partway through an apply.

## Schedule

A cron prompt sent to a Teammate. `cron` is five or six fields in UTC, and
FTN020 rejects an expression fountain would store and then never fire. The
`@daily`-style shorthands are refused with it: chant validates and matches one
cron notion, the five- or six-field one, so write the fields out. `one_off:
false` sends the prompt into the teammate's own thread, where a busy teammate
means the run is skipped rather than queued; `one_off: true` opens a fresh
conversation on each fire.

## Webhook

A URL fountain POSTs lifecycle events to. FTN022 requires https and refuses
loopback, link-local and RFC1918 targets, which fountain refuses again at
request time. The signing secret is returned once at create and is never
readable afterwards, so it is not part of the declared shape.

## Example

```ts
import { Environment, Agent, Teammate, Schedule, Webhook } from "@intentius/chant-lexicon-fountain";

export const conciergeEnv = new Environment({
  name: "concierge-env",
  networking_type: "limited",
  networking_config: { allowed_hosts: ["registry.npmjs.org", "github.com"] },
  metadata: { "managed-by": "chant" },
});

export const researcher = new Agent({
  name: "researcher",
  model: "anthropic/claude-sonnet-4-6",
  runtime: "claude",
  environment: conciergeEnv,
  skills: [{ source: "vercel-labs/agent-skills", ref: "main" }],
});

export const researcherSeat = new Teammate({
  name: "researcher",
  agent: researcher,
});

export const nightlySweep = new Schedule({
  name: "nightly-sweep",
  teammate: researcherSeat,
  cron: "0 3 * * *",
  prompt: "chant run sweep",
});

export const turnEvents = new Webhook({
  url: "https://example.com/hooks/fountain",
  event_types: ["conversation.turn.done", "conversation.turn.failed"],
});
```
