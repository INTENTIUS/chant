// Code generated by chant slurm generate. DO NOT EDIT generated sections.

import type { ResourceAttributes } from "@intentius/chant/runtime";

export declare class Job {
  constructor(props: {
    /** Charge resources used to this account */
    account?: string;
    /** Job array specification (e.g. 1-10%4 — indices with max concurrent) */
    array?: string;
    /** Arbitrary comment attached to the job */
    comment?: string;
    /** Node feature constraints (e.g. efa,gpu) */
    constraints?: string;
    /** CPUs required per task */
    cpus_per_task?: number;
    /** Working directory for the job */
    current_working_directory?: string;
    /** Environment variables as key-value pairs */
    environment?: Record<string, string>;
    /** Allocate nodes exclusively (no sharing with other jobs) */
    exclusive?: boolean;
    /** Generic resource (GRES) required per node (e.g. gpu:a100:8) */
    gres_per_node?: string;
    /** License requirements (e.g. vcs_sim:4,calibre:2) */
    licenses?: string;
    /** Email address(es) for job notifications */
    mail_user?: string;
    /** Memory required per CPU in megabytes */
    memory_per_cpu?: number;
    /** Memory required per node in megabytes */
    memory_per_node?: number;
    /** Job name */
    name?: string;
    /** Number of nodes to allocate */
    node_count?: number;
    /** Specific nodes to use (node list expression) */
    nodes?: string;
    /** Partition to submit the job to */
    partition?: string;
    /** Job priority override (0–65534; 0 = hold) */
    priority?: number;
    /** Quality of Service name */
    qos?: string;
    /** Batch job script content (must start with #!/...) */
    script?: string;
    /** Path for job stderr (supports %j for job ID) */
    standard_error?: string;
    /** Path for job stdout (supports %j for job ID) */
    standard_output?: string;
    /** Total number of tasks (MPI ranks) */
    task_count?: number;
    /** Time limit in minutes (0 = unlimited, use partition default) */
    time_limit?: number;
  }, attributes?: ResourceAttributes);
}

export declare class Reservation {
  constructor(props: {
    /** Reservation name (must be unique) */
    name: string;
    /** Comma-separated accounts allowed to use the reservation */
    accounts?: string;
    /** Duration in minutes (alternative to end_time) */
    duration?: number;
    /** End time (ISO 8601, 'now+Nd', etc.) */
    end_time?: string;
    /** Reservation flags (e.g. MAINTENANCE,FLEX) */
    flags?: string;
    /** Number of nodes to reserve */
    node_count?: number;
    /** Specific node list expression to reserve */
    node_list?: string;
    /** Partition the reservation is limited to */
    partition?: string;
    /** Start time (ISO 8601 or SLURM_TIME_EPOCH or 'now') */
    start_time?: string;
    /** Comma-separated users allowed to use the reservation */
    users?: string;
  }, attributes?: ResourceAttributes);
}

export declare class QoS {
  constructor(props: {
    /** QoS name (unique identifier) */
    name: string;
    /** Human-readable description of this QoS */
    description?: string;
    /** Maximum total CPUs for all jobs in this QoS simultaneously */
    grp_cpus?: number;
    /** Maximum total nodes for all jobs in this QoS simultaneously */
    grp_nodes?: number;
    /** Maximum CPUs a single user can allocate under this QoS */
    max_cpus_per_user?: number;
    /** Maximum nodes a single user can allocate under this QoS */
    max_nodes_per_user?: number;
    /** Maximum wall clock time per job in seconds */
    max_wall_clock_per_job?: number;
    /** Preemption mode: CANCEL, CHECKPOINT, GANG, REQUEUE, SUSPEND, or OFF */
    preempt_mode?: string;
    /** QoS priority (higher → higher scheduling priority) */
    priority?: number;
  }, attributes?: ResourceAttributes);
}
