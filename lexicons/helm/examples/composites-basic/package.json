{
  "name": "helm-composites-basic",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "build": "chant build src --lexicon helm",
    "lint": "chant lint src",
    "dev": "chant build src --lexicon helm --watch",
    "deploy": "helm install composites-basic .",
    "teardown": "helm uninstall composites-basic"
  },
  "dependencies": {
    "@intentius/chant": "workspace:*",
    "@intentius/chant-lexicon-helm": "workspace:*",
    "@intentius/chant-lexicon-k8s": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
