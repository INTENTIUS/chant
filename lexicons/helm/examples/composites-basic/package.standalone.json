{
  "name": "helm-composites-basic",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "build": "chant build src --lexicon helm",
    "lint": "chant lint src",
    "dev": "chant build src --lexicon helm --watch",
    "deploy": "helm install composites-basic .",
    "teardown": "helm uninstall composites-basic"
  },
  "dependencies": {
    "@intentius/chant": "^0.0.14",
    "@intentius/chant-lexicon-helm": "^0.0.14",
    "@intentius/chant-lexicon-k8s": "^0.0.14"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
