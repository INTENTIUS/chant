apiVersion: v2
name: genvalues-fixture
description: >-
  Survey fixture: an open generated input (`randAlphaNum`) that cascades into a
  derived checksum annotation (epic finding 6). Unstable across renders as-is;
  byte-identical once the password is supplied as a values input. Pin the
  input, not the output.
type: application
version: 0.1.0
