{
  "name": "shared-alb-ui-example",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "build": "chant build src --lexicon aws -o template.json",
    "lint": "chant lint src",
    "dev": "chant build src --lexicon aws --watch",
    "deploy": "aws cloudformation deploy --template-file template.json --stack-name shared-alb-ui --capabilities CAPABILITY_NAMED_IAM",
    "teardown": "aws cloudformation delete-stack --stack-name shared-alb-ui"
  },
  "dependencies": {
    "@intentius/chant": "^0.0.14",
    "@intentius/chant-lexicon-aws": "^0.0.14"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
