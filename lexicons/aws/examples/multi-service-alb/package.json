{
  "name": "multi-service-alb-example",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "build": "chant build src --lexicon aws -o template.json",
    "lint": "chant lint src",
    "dev": "chant build src --lexicon aws --watch -o template.json",
    "deploy": "aws cloudformation deploy --template-file template.json --stack-name multi-service-alb --capabilities CAPABILITY_NAMED_IAM",
    "teardown": "aws cloudformation delete-stack --stack-name multi-service-alb"
  },
  "dependencies": {
    "@intentius/chant-lexicon-aws": "workspace:*",
    "@intentius/chant": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
