# Default recipe - list all available commands
default:
    @just --list

# Generate types and metadata from CloudFormation schemas
generate:
    npx tsx src/codegen/generate-cli.ts

# Validate generated artifacts
validate:
    npx tsx src/validate-cli.ts

# Generate docs site, install deps, and start dev server
docs:
    DOCS_BASE_PATH=/ npx tsx src/codegen/docs-cli.ts
    npm install --prefix docs
    npm run --prefix docs dev --port 4322

# Build docs site for production
docs-build:
    DOCS_BASE_PATH=/ npx tsx src/codegen/docs-cli.ts
    npm install --prefix docs
    npm run --prefix docs build

# Run CloudFormation samples roundtrip import test
import-samples *args:
    bash scripts/import-aws-samples.sh {{args}}

# Diff the curated enum overlay against its recorded sources (network; never part of a build)
refresh-enum-overlay *args:
    npx tsx scripts/refresh-enum-overlay.ts {{args}}

# Package the lexicon (generate + validate)
package: generate validate
