{
  "$comment": "Screen spec for the app's home page. The app member implements it in app/src/server.mjs. Decision ref-002 pins this file by hash, so an edit shows as drift in chant workspace records until the decision is revisited and re-pinned.",
  "screen": "home",
  "route": "/",
  "title": "{{chant:name}}",
  "regions": [
    { "id": "header", "element": "header", "contains": [{ "element": "h1", "text": "{{chant:name}}" }] },
    { "id": "status", "element": "main", "contains": [{ "element": "p", "id": "status", "text": "Running." }] }
  ],
  "wireframe": "home.svg"
}
