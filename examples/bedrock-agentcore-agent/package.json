{
  "name": "bedrock-agentcore-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The composite/base path for #882: a Bedrock AgentCore agent (Runtime + Memory + Gateway/GatewayTarget + WorkloadIdentity + IAM) as one CloudFormation stack, deployed with cfn-deploy + wait-for-stack — no bespoke verb.",
  "devDependencies": {
    "@intentius/chant": "*",
    "@intentius/chant-lexicon-aws": "*",
    "typescript": "*"
  },
  "scripts": {
    "build": "chant build src --lexicon aws -o dist/agent.template.json",
    "lint": "chant lint src",
    "deploy": "chant run --components all --env local --no-release-record",
    "status": "chant components status local --live --no-release-record",
    "teardown": "aws cloudformation delete-stack --stack-name bedrock-agentcore-agent"
  }
}
