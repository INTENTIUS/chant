{
  "name": "ray-kuberay-gke-example",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "bootstrap": "bash scripts/bootstrap.sh",
    "build": "chant build src --lexicon gcp -o config.yaml && chant build src --lexicon k8s -o k8s.yaml",
    "build:gcp": "chant build src --lexicon gcp -o config.yaml",
    "build:k8s": "chant build src --lexicon k8s -o k8s.yaml",
    "build:k3d": "chant build k3d/src --lexicon k8s -o k3d/k3d.yaml",
    "lint": "chant lint src",
    "deploy-infra": "kubectl apply -f config.yaml",
    "configure-kubectl": "gcloud container clusters get-credentials ${GKE_CLUSTER_NAME:-ray-gke} --region ${GCP_REGION:-us-central1} --project ${GCP_PROJECT_ID}",
    "install-operator": "helm repo add kuberay https://ray-project.github.io/kuberay-helm/ && helm repo update && helm upgrade --install kuberay-operator kuberay/kuberay-operator -n kuberay-operator --create-namespace --version 1.3.2 && kubectl -n kuberay-operator wait deploy/kuberay-operator --for=condition=Available --timeout=120s",
    "apply": "kubectl apply -f k8s.yaml",
    "status": "kubectl -n ray-system get raycluster ray && kubectl -n ray-system get pods -l ray.io/cluster-name=ray",
    "dashboard": "kubectl -n ray-system port-forward svc/ray-head-svc 8265:8265",
    "deploy": "bash scripts/deploy.sh",
    "teardown": "bash scripts/teardown.sh"
  },
  "dependencies": {
    "@intentius/chant": "workspace:*",
    "@intentius/chant-lexicon-gcp": "workspace:*",
    "@intentius/chant-lexicon-k8s": "workspace:*"
  }
}
