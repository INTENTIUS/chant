node_modules/
dist/
*.tsbuildinfo
*.tgz
.DS_Store
.cache/

# All generated lexicon source (covers both AWS and GitLab)
lexicons/*/src/generated/

# Generated lexicon docs sites — ignore build output but track source
lexicons/*/docs/node_modules/
lexicons/*/docs/dist/
lexicons/*/docs/.astro/
lexicons/*/docs/bun.lock
.docs-dist

# Example .env files (generated by `bun run load-outputs`)
examples/*/.env
examples/*/.cert-arn

# Example build outputs
examples/*/k8s.yaml
examples/*/templates/*.json
examples/*/.gitlab-ci.yml
examples/*/.github/

# supply-chain example: generated SBOM/config-BOM (npm run supply-chain)
examples/supply-chain/sbom.*.json
examples/supply-chain/config-bom.*.json

# Docker smoke build artifacts
test/example-builds/

# Claude Code
.claude/
CLAUDE.md

# Scratch project the fountain init-template test materializes (cleaned up in afterAll)
lexicons/fountain/.init-template-*/

# chant#2425 — the specification checkout the docs citation gate loads its
# check from, until @intentius/tsad-conformance is on npm.
.tsad/
